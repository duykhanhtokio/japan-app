# Placeholder assets

The image files in this directory are technical build placeholders generated
because the code-only source archive did not include its original `assets`
directory. They preserve every static `require(...)` path so Metro can bundle
the app on web, iOS, and Android.

Replace each placeholder with the final PNG/JPG using the same path and file
name. No TypeScript changes are required when final art is added.
