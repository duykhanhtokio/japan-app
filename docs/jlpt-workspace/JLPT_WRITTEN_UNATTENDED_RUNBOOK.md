# Deprecated compatibility pointer

The written-only workflow is superseded by `JLPT_UNATTENDED_RUNBOOK.md`. `scripts/run-jlpt-written-unattended.sh` remains only as a compatibility wrapper around the general supervisor.
