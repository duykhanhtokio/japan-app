# Deprecated compatibility pointer

The written-only unattended prompt has been superseded by `JLPT_UNATTENDED_PROMPT.md`. The active supervisor and all future child turns must use that general prompt; listening candidate work is now authorized with unverified metadata.
