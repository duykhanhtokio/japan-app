# N5 2021-12 iPhone runtime correction

User reported on 2026-09-26 that the listening illustration overflowed the iPhone viewport and audio could not be heard. The supplied simulator screenshot showed the second listening illustration clipped horizontally, the playback controls outside the visible scroll position, and a misleading N1 header. At 23:30 JST the user explicitly authorized modifying the locked JLPT UI and updating its SHA after checks.

The shared exam runner changed only for these reported defects: constrain question images to the paper width and preserve each illustration's aspect ratio; keep one full-track play/pause control visible for N5 2021-12; enable iOS silent-mode playback, check audio loading before seeking, and show playback errors; derive the header level from the exam data. The track is still continuous. Answers and transcripts remain hidden before submission. All other locked files remain byte-identical.

The lock checker SHA for `N1OfficialTrial.tsx` was refreshed only after this explicit user-requested UI correction. This is a new lock baseline, not an exemption for future edits. The user confirmed that the LFS MP3 has 13,850,975 bytes and the 15 JPEG files are valid. The correction still needs an iPhone runtime check after pulling the branch to confirm the layout and audible playback.
