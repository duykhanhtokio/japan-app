import fs from 'node:fs';
import path from 'node:path';
const root = process.cwd();
const registry = fs.readFileSync(path.join(root,'src/data/jlpt-official/approved-n1-exams.ts'),'utf8');
const catalog = fs.readFileSync(path.join(root,'src/data/jlpt-official/jlpt-exam-catalog.ts'),'utf8');
const structuredIds = [...registry.matchAll(/id: '(n[1-5]-(\d{4})-(07|12)-exam-\d+)'/g)].map(m=>m[1]);
const pendingIds = [...catalog.matchAll(/'(n[1-5]-(\d{4})-(07|12))'/g)].map(m=>m[1]);
const rows = structuredIds.map((examId)=>{const [level,year,month]=examId.split('-');return {level:level.toUpperCase(),period:`${year}-${month}`,examId,questionText:true,options:true,correctAnswers:true,passages:true,transcripts:true,audio:true,audioBoundaries:true,renderer:'N1OfficialTrial',status:'structured_ready',sourceModule:`src/data/jlpt-official/${level}-${year}-${month}/`};});
const n5AuditedAudio = new Set(['n5-2011-12','n5-2012-12','n5-2013-07','n5-2017-07','n5-2018-12','n5-2020-12','n5-2021-12']);
for(const examId of pendingIds){const [level,year,month]=examId.split('-'),external=level==='n4'||level==='n5',audited=external,audioAudited=(level==='n4'&&(pendingIds.indexOf(examId)<6||pendingIds.indexOf(examId)>=7))||n5AuditedAudio.has(examId),status=audited?'incomplete':'scanned_only';rows.push({level:level.toUpperCase(),period:`${year}-${month}`,examId,questionText:false,options:false,correctAnswers:false,passages:false,transcripts:false,audio:audioAudited,audioBoundaries:false,renderer:'none (source package pending structured conversion)',status,sourceModule:external?`external-source:${level.toUpperCase()}/${year}-${month}`:`src/data/jlpt-mock/${examId}-official.ts`});}
for (const level of ['N1','N2','N3','N4','N5']) rows.push({level,period:'mock-01',examId:`${level.toLowerCase()}-mock-01`,questionText:true,options:true,correctAnswers:true,passages:true,transcripts:true,audio:false,audioBoundaries:false,renderer:'ApprovedMockExam',status:'mock_ready',sourceModule:'src/data/jlpt-mock/sample-exams.ts'});
rows.sort((a,b)=>a.level.localeCompare(b.level)||a.period.localeCompare(b.period));
const summary={catalogTotal:rows.length,totalSourcePackages:rows.length,structuredReady:rows.filter(r=>r.status==='structured_ready').length,converting:rows.filter(r=>r.status==='converting').length,scannedOnly:rows.filter(r=>r.status==='scanned_only').length,incomplete:rows.filter(r=>r.status==='incomplete').length,notVerified:rows.filter(r=>r.status==='not_verified').length,mockReady:rows.filter(r=>r.status==='mock_ready').length,readyTotal:rows.filter(r=>r.status==='structured_ready'||r.status==='mock_ready').length,visibleTotal:rows.length};
fs.mkdirSync(path.join(root,'docs/jlpt-workspace'),{recursive:true}); fs.writeFileSync(path.join(root,'docs/jlpt-workspace/JLPT_50_EXAMS_TRUTHFUL_INVENTORY.json'),`${JSON.stringify({generatedAt:new Date().toISOString(),summary,exams:rows},null,2)}\n`); console.log(summary);
