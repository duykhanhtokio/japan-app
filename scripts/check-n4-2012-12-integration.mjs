import assert from 'node:assert/strict';
import { createHash } from 'node:crypto';
import fs from 'node:fs';
const manifest=JSON.parse(fs.readFileSync('docs/jlpt-workspace/conversion/n4-2012-12/WORK_MANIFEST.json'));
const catalog=fs.readFileSync('src/data/jlpt-official/jlpt-exam-catalog.ts','utf8');
const hash=p=>createHash('sha256').update(fs.readFileSync(p)).digest('hex');
assert.equal(manifest.examId,'n4-2012-12-exam-02');
assert.equal(manifest.status,'incomplete');
assert.deepEqual(manifest.counts,{writtenResponsesObserved:70,listeningResponsesObserved:28});
assert.equal(manifest.source.answerKeyPresent,true);
assert.equal(manifest.source.chineseExplanationsAndTranslationsPresent,true);
assert.equal(manifest.source.japaneseListeningTranscriptPresent,true);
assert.equal(manifest.source.sourceTimingPresent,false);
assert.equal(manifest.review.humanReviewed,false);
assert.equal(manifest.review.perceptualApproval,false);
assert.equal(manifest.review.reviewDisposition,'needs_later_review');
assert.match(catalog,/'n4-2012-12'/);
assert.doesNotMatch(catalog,/n4-2012-12-exam-02/);
assert.equal(hash('assets/jlpt/n4/2012-12/audio/n4-2012-12.mp3'),manifest.runtimeAudio.sha256);
console.log('N4 2012-12 INTEGRATION AUDIT PASS: 70 written and 28 listening responses inventoried; source components recorded; exam remains incomplete and is not structured_ready.');
